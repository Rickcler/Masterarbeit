\documentclass{beamer}

\usepackage{tikz}
\usetikzlibrary{positioning}
\usepackage{graphicx}
\usepackage{amsmath, amssymb}
\usepackage{cleveref}
\usepackage{xcolor}
% -------------------------------------------------------
% Section header on every content slide (top-right corner)
% -------------------------------------------------------

\setbeamertemplate{headline}{%
  \begin{beamercolorbox}[wd=\paperwidth, ht=4ex, dp=1.5ex, rightskip=0.3cm]{section in head/foot}%
    \hfill\small\textit{\insertsectionhead}\hspace{0.3cm}
  \end{beamercolorbox}%
}
\setbeamertemplate{footline}{
  \hfill
  \insertframenumber{} / \inserttotalframenumber
  \hspace{1em}
}
\title{Inference for Categorical Time Series with Missing Observations}
\author{Victor Minig}
\date{27.04.2026}

\begin{document}

\frame{\titlepage}

%------------------------------------------------
\begin{frame}{Outline}
\tableofcontents
\end{frame}

%================================================
\section{Categorical Time Series}
%================================================

\begin{frame}{Motivation}
\begin{itemize}
    \item Many real-world time series are \textbf{categorical}:
    \begin{itemize}
        \item Credit ratings (AAA, AA, ...)
        \item Weather states (sunny, rainy, ...)
        \item Survey responses (agree, neutral, disagree)
    \end{itemize}
    
    \vspace{0.3cm}
    
    \item Classical time series methods assume \textbf{numeric structure}
    
    \vspace{0.3cm}
    
    \item $\Rightarrow$ Need new tools for:
    \begin{itemize}
        \item Computing proper tests for marginal features 
        \item Measuring temporal dependence without bias 
    \end{itemize}
\end{itemize}
\end{frame}

%------------------------------------------------
\begin{frame}{Categorical Time Series}
\begin{block}{Definition}
A categorical time series $(X_t)$ takes values in a finite set
\[
S = \{s_0, \dots, s_m\},
\]
\hspace{2.5cm}e.g. \raisebox{-0.38\height}{\includegraphics[width=0.4\textwidth]{../Graphs/weather_symbols.png}}
\end{block}

\vspace{0.3cm}

\begin{block}{Serial Dependence}
In many applications, observations are not independent over time:
\begin{itemize}
    \item The distribution of $X_t$ may depend on past values $X_{t-1}, X_{t-2}, \dots$
    
\end{itemize}
\end{block}

\textbf{Key implication:}
\begin{itemize}
    \item Available statistical tools depend on both the structure of $S$ and the dependence over time
\end{itemize}
\end{frame}

%------------------------------------------------
\begin{frame}{Ordinal vs. Nominal Data}



\textbf{Nominal data} (e.g. eye color, gender): 

\begin{columns}
    \begin{column}{0.5\textwidth}
        \begin{itemize}
            \item No natural ordering of categories
        \end{itemize}
    \end{column}
        \begin{column}{0.5\textwidth}
        \includegraphics[width=0.4\textwidth]{../Graphs/Nominal.png}
    \end{column}
\end{columns}
\vspace{0.5cm}

\textbf{Ordinal data} (e.g. education level, satisfaction ratings):

\begin{columns}
    \begin{column}{0.5\textwidth}
        \begin{itemize}
            \item Elememts of $S$ have a natural ordering
            \item However, intervals between categories are not meaningful
        \end{itemize}
    \end{column}
        \begin{column}{0.5\textwidth}
        \includegraphics[width=0.7\textwidth]{../Graphs/Ordinal.png}
    \end{column}
\end{columns}
\vspace{0.5cm}\vspace{0.3cm}



\end{frame}


%------------------------------------------------
\begin{frame}{Marginal Distributions}

\textbf{PMF}(Probability Mass Function): 
\begin{columns}
    \begin{column}{0.5\textwidth}
        \begin{align*}
            &p_i = P(X_t = s_i) \\
            \Rightarrow &\boldsymbol{p} = (p_0, p_1, \dots, p_m)^{\top}
        \end{align*}
    \end{column}
        \begin{column}{0.5\textwidth}
        \includegraphics[width=1\textwidth]{../Graphs/PMF_weather.png}
    \end{column}
\end{columns}
\vspace{0.5cm}


\textbf{CDF}(Cumulative Distribution Function):
\begin{columns}
    \begin{column}{0.5\textwidth}
        \begin{align*}
            &f_i = P(X_t \leq s_i) \\
            \Rightarrow &\boldsymbol{f} = (f_0, f_1, \dots, f_m)^{\top}
        \end{align*}
    \end{column}
        \begin{column}{0.5\textwidth}
        \includegraphics[width=1\textwidth]{../Graphs/CDF_weather.png}
    \end{column}
\end{columns}

\end{frame}


%------------------------------------------------
\begin{frame}{Location Measures}

\begin{columns}
    \begin{column}{0.5\textwidth}
        \textbf{Mode:}
            \[
            \text{Mod}(X_t) = \arg\max_{s_i} P(X_t = s_i)
            \]
    \end{column}
    \begin{column}{0.5\textwidth}
        \textbf{Median:}
        \[
        \text{Med}(X_t) = \min \{ s_i : F(s_i) \geq 1/2 \}
        \]
    \end{column}
\end{columns}
\hspace{1.7cm}\includegraphics[width=0.7\textwidth, height =0.3\textheight]{../Graphs/mode_median.png}

\textbf{Interpretation:}
\begin{itemize}
    \item Mode = most frequent category
    \item Median = central position in ordering
\end{itemize}
\end{frame}

%------------------------------------------------
\begin{frame}{Dispersion}
\textbf{Minimal Dispersion:}
\vspace{0.2cm}
\hspace{2.5cm}\includegraphics[width=0.5\textwidth, height =0.12\textheight]{../Graphs/minimal_dispersion.png}
    

\textbf{Maximal Dispersion:}
\begin{columns}
    \begin{column}{0.5\textwidth}
        \includegraphics[width=1\textwidth, height =0.15\textheight]{../Graphs/ordinal_maximal_dispersion.png}
    \end{column}
    \begin{column}{0.5\textwidth}
        \includegraphics[width=1\textwidth, height =0.15\textheight]{../Graphs/nominal_maximal_dispersion.png}
    \end{column}
\end{columns}

\vspace{0.4cm}

\noindent
\begin{minipage}[t]{0.48\textwidth}
    \textbf{Ordinal}
    \[
        \mathrm{IOV}(\boldsymbol{f}) 
        = \frac{4}{m} \sum_{i=0}^{m-1} f_i(1-f_i)
    \]
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\textwidth}
    \textbf{Nominal}
    \[
        \mathrm{IQV}(\boldsymbol{p}) 
        = \frac{m+1}{m}\!\left(1 - \sum_{i=0}^{m} p_i^2\right)
    \]
\end{minipage}

\vspace{0.4cm}

\textbf{From here on:}
We focus on \textbf{ordinal data}, unless stated otherwise.
\end{frame}

%------------------------------------------------
\begin{frame}{Stationarity}
\textbf{Why do we need stationarity?}

\begin{itemize}
    \item Stable probabilistic structure over time
    \item Enables estimation and asymptotics
\end{itemize}

\vspace{0.5cm}

\begin{block}{Strict Stationarity}
\[
F_{X_t, \dots, X_{t+j}} = F_{X_{t+h}, \dots, X_{t+h+j}}
\]
for all $t, j, h$.
\end{block}

\vspace{0.3cm}

\begin{itemize}
    \item Distribution invariant under time shifts
\end{itemize}
\end{frame}

%------------------------------------------------
\begin{frame}{Temporal Dependence}
\textbf{Goal: capture dependence across time}

\vspace{0.3cm}

\textbf{Problem:}
\begin{itemize}
    \item Classical ACF not applicable
\end{itemize}

\vspace{0.3cm}

\textbf{Solution:}
\vspace{0.1cm}


\begin{columns}
    \begin{column}{0.5\textwidth}
        \begin{block}{Bivariate Probabilities}
            \vspace{-0.5cm}
            \begin{align*}
                &p_{ij}(h) = P(X_t = s_i, X_{t+h} = s_j) \\
                &f_{ij}(h) = P(X_t \leq s_i, X_{t+h} \leq s_j)\\
                &\textbf{Bei Unabhängigkeit: } \\\\
                &p_{ij}(h) = p_i p_j \quad \text{und} \quad  f_{ij}(h) = f_i f_j
            \end{align*}
        \end{block}
    \end{column}
    \begin{column}{0.5\textwidth}
        \includegraphics[width=\textwidth]{../Graphs/bivariate_matrix.png}
    \end{column}    
\end{columns}




\end{frame}

%------------------------------------------------
\begin{frame}{Cohen's $\kappa$}
\textbf{Idea: Standardized measure of deviation from independence}

\vspace{0.5cm}

\[
\kappa(h)_{\mathrm{ord}} =
\frac{\sum_{j=0}^{m} (f_{jj}(h) - f_j^2)}
{1 - \sum_{i=0}^{m} f_i(1 - f_i)}
\]

\vspace{0.5cm}

\begin{itemize}
    \item $\kappa = 1$: perfect persistence
    \item $\kappa = 0$: independence
    \item $\kappa < 0$: tendency to switch states
\end{itemize}

\vspace{0.5cm}

\textbf{Interpretation:}
\begin{itemize}
    \item Probability of staying in same category
    \item Relative to independence benchmark
\end{itemize}
\end{frame}

%================================================
\section{Estimation — Complete Data}
%================================================

%------------------------------------------------
\begin{frame}{From Theory to Estimation}
\textbf{All quantities defined so far are population quantities.}

\vspace{0.4cm}

\begin{block}{Central question}
How do we estimate $\boldsymbol{f}$, IOV, and $\kappa_{\mathrm{ord}}(h)$ from data — and what are the statistical properties of these estimators?
\end{block}

\vspace{0.4cm}

\textbf{Two steps:}
\begin{enumerate}
    \item Estimate marginal CDF $\boldsymbol{f}\text{(/} \boldsymbol{p}\text{)}$ (and later the bivariate probabilities) via relative frequencies
    \item Derive (asymptotic) distributions of plug-in estimators using a \textbf{CLT} and the \textbf{delta method}
\end{enumerate}

\vspace{0.3cm}

\end{frame}

%------------------------------------------------
\begin{frame}{Estimation of Marginal Probabilities}
\textbf{Idea: use of indicator functions}

\vspace{0.3cm}

\[
Y_{t,i} = \mathbb{I}\{X_t = s_i\} \quad \text{and} \quad Z_{t,i} = \mathbb{I}\{X_t \leq s_i\}
\]
Then estimators are given by:
\[
    \hat{p}_i = \frac{1}{n} \sum_{t=1}^n Y_{t,i} 
    \quad \text{and} \quad 
    \hat{f}_i = \frac{1}{n} \sum_{t=1}^n Z_{t,i}
\]

\vspace{0.2cm}

\hspace{2.5cm} \includegraphics[width=1\textwidth, height = 0.4 \textheight]{../Graphs/marginal_estimation.png}
\end{frame}
%------------------------------------------------
\begin{frame}{Deriving Asymptotic Distributions}

\begin{block}{Goal}
We aim to derive asymptotic distributions for estimators based on categorical time series for inference.
\end{block}

\vspace{0.3cm}

\begin{block}{Key tool}
The \textbf{Central Limit Theorem (CLT)} provides asymptotic normality:
\[
\sqrt{n}(\hat{\theta} - \theta) \Rightarrow \mathcal{N}(0, \sigma^2)
\]
\end{block}

\vspace{0.3cm}

\begin{alertblock}{Problem}
Classical CLTs require \textbf{independent observations}, but time series data typically exhibit \textbf{serial dependence}:
\begin{itemize}
    \item Current observations depend on past values
    \item This dependence persists across time lags
\end{itemize}
\end{alertblock}

\vspace{0.3cm}

\end{frame}

%------------------------------------------------
\begin{frame}{Mixing Conditions}

\begin{block}{Idea}
We control serial dependence via \textbf{mixing conditions}, which quantify how fast dependence between past and future decays.
\end{block}

\vspace{0.4cm}

\begin{block}{$\alpha$-mixing (strong mixing)}
Let $\mathcal{F}_{-\infty}^t = \sigma(X_s : s \leq t)$ and $\mathcal{F}_{t+h}^{\infty} = \sigma(X_s : s \geq t+h)$.\\

The $\alpha$-mixing coefficients are defined as:
\[
\alpha(h) = \sup_{t \in \mathbb{Z}} \sup_{A \in \mathcal{F}_{-\infty}^t,\; B \in \mathcal{F}_{t+h}^{\infty}}
\big| P(A \cap B) - P(A)P(B) \big|
\]
\end{block}

\vspace{0.3cm}

\begin{itemize}
    \item $\alpha(h) \overset{\tiny h \to \infty}{\rightarrow} 0$: dependence between past and future vanishes
    \item Exponential decay $\Rightarrow$ CLT for dependent data
\end{itemize}

\end{frame}
%------------------------------------------------
\begin{frame}{CLT under Serial Dependence}
    \begin{block}{Result}
    \[\sqrt{n}(\hat{\boldsymbol{f}} - \boldsymbol{f}) \Rightarrow \mathcal{N}(0, \boldsymbol{\Sigma})\]
    where the elements of $\boldsymbol{\Sigma}$ are given by:
    \[\sigma_{ij} = \underbrace{\left(f_{\min\{i,j\}}- f_i f_j\right)}_{\text{lag}(0)} + \sum_{h=1}^{\infty} \underbrace{\left(f_{ij}(h) + f_{ji}(h) - 2f_if_j\right)}_{\text{lag}(h)}.\]
    Note that $\text{lag}(h) \rightarrow 0$ as $h \rightarrow \infty$.
    \end{block}
\end{frame}

%------------------------------------------------
\begin{frame}{CLTs for Marginal Estimators}
    \begin{block}{Idea}
        IOV is a function of the marginal probabilities \\
        $\Rightarrow$ apply the \textbf{delta method} to obtain asymptotic distributions 
    \end{block}
    \begin{block}{Results}
        \begin{itemize}
            \item Asymptotic variance: 
            \[\mathrm{Var}(\sqrt{n}\cdot\widehat{\mathrm{IOV}}) \overset{\mathrm{asy}}{=} \frac{16}{m^2}\sum_{i,j = 0}^{m-1} (1 - 2f_i)(1 - 2f_j) \sigma_{ij}\]
            \item Bias to order $n^{-1}$:
            \[ \mathrm{Bias}(n\cdot\widehat{\mathrm{IOV}}) = -\frac{4}{m} \sum_{i = 0}^{m-1}  \sigma_{ii}\]
        \end{itemize}
    \end{block}
\end{frame}

%------------------------------------------------

\begin{frame}{Scaling of Variance and Bias}

\centering
\begin{tikzpicture}[node distance=1cm and 0.5cm]


% --- Top row ---
\node (p1) {\includegraphics[width=0.4\textwidth, height = 3cm]{../Graphs/Var_Presi_no_n.png}};
\node (a1) [right=of p1] {$\xrightarrow{\cdot \sqrt{n}}$};
\node (p2) [right=of a1] {\includegraphics[width=0.4\textwidth, height = 3cm]{../Graphs/P!.png}};
% --- Title ---
\node (t1) [above = 0.5cm of a1] {\small\textbf{Variance}};

% --- Bottom row ---
\node (p3) [below=of p1] {\includegraphics[width=0.4\textwidth, height = 3cm]{../Graphs/Bias_presi_no_n.png}};
\node (a2) [right=of p3] {$\xrightarrow{\cdot n}$};
\node (p4) [right=of a2] {\includegraphics[width=0.4\textwidth, height = 3cm]{../Graphs/Bias_Presi_.png}};
% --- Title ---
\node (t2) [above = 0.5cm of a2] {\small\textbf{Bias}};
\end{tikzpicture}

\end{frame}

%================================================
\section{Missing Data}
%================================================

%------------------------------------------------
\begin{frame}{Why Does Missingness Matter?}

\textbf{Missing observations are ubiquitous in practice:}
\begin{itemize}
    \item Panel surveys: participants drop out over time
    \item Sensor data: transmission errors, device downtime
\end{itemize}

\vspace{0.4cm}

\textbf{Problem}

\begin{itemize}
    \item Simply ignoring missing observations leads to biased estimates of standard errors and plug-in estimators
\end{itemize}

\vspace{0.4cm}

\textbf{Goal:}
\begin{itemize}
    \item Correct for missingness — under suitable assumptions
    \item Derive valid asymptotic theory for the corrected estimators
\end{itemize}
\end{frame}

%------------------------------------------------
\begin{frame}{Amplitude Modulation}
    \begin{block}{Idea}
        Missingness can be modeled as a binary process $(O_t)$, that \textit{modulates} the \textit{amplitude} of the actual process: 
        \[O_t = 1 \text{ if } X_t \text{ is observed and } O_t = 0 \text{ if } X_t \text{ is missing}\] 
        The observed process is then given by $(O_t X_t)$.
    \end{block}
    \begin{block}{Assumptions}
        \begin{itemize}
            \item $(O_t)$ is stationary, with $P(O_t = 1) = \pi$ 
            and joint moments $E[O_t O_{t+h}] = \pi(h)$ (if $O_t$ is i.i.d., then $\pi(h) = \pi^2$)
            \item $(O_t)$ is $\alpha$-mixing with exponential decay
        \end{itemize}
    \end{block}    
\end{frame}

%------------------------------------------------
\begin{frame}{Dependence structure of $(O_t)$ and $(X_t)$}
    \begin{itemize}  
        \item \textbf{Indepedence:} $(\sim\!\text{MCAR})$
        \[P(O_t = 1| X_t, X_{t-1}, \ldots)\ = P(O_t = 1)\]
        \item \textbf{Dependendence on past states:} $(\sim\!\text{MCAR})$
        \[P(O_t = 1| X_t, X_{t-1}, \ldots)\ = P(O_t = 1| X_{t-1}, \ldots)\]
        \item \textbf{Dependence on current state:} $(\sim\!\text{MCAR})$
        \[P(O_t = 1| X_t, X_{t-1}, \ldots)\ \neq P(O_t = 1| X_{t-1}, \ldots)\]
    \end{itemize}
    From here on, unless stated otherwise, we assume Independence.
\end{frame}

%------------------------------------------------
\begin{frame}{Naive vs.\ Corrected Estimators}
    \begin{block}{Naive estimator}
        \[\hat{\boldsymbol{\mathfrak{f}}} = \frac{1}{n} \sum_{t=1}^n O_t \boldsymbol{Z}_t\]
        $\Rightarrow$ biased and inconsistent: $E[\hat{\mathfrak{f}}_i] = \pi f_i \neq f_i$
    \end{block}
    \begin{block}{Corrected estimator}
        \[\hat{\boldsymbol{f}}^* = \frac{\frac{1}{n}\sum_{t=1}^n O_t \boldsymbol{Z}_t}{\frac{1}{n}\sum_{t=1}^n O_t}\]
        $\Rightarrow$ divide by number of observed time points, not by $n$
    \end{block}
\end{frame}

%------------------------------------------------
\begin{frame}{Asymptotic Properties of the Corrected Estimator}
    \begin{block}{Result}
        \[\sqrt{n}(\hat{\boldsymbol{f}}^* - \boldsymbol{f}) \Rightarrow \mathcal{N}(0, \boldsymbol{\Sigma}^*)\]
        where the elements of $\boldsymbol{\Sigma}^*$ are given by:
        \[
            \sigma^*_{ij} =
            \textcolor{blue}{\frac{1}{\pi}} (f_{\mathrm{min}\{i,j\}} - f_i f_j)
            +
            \textcolor{blue}{\frac{1}{\pi^2}} 
            \sum_{h = 1}^{\infty} 
            \textcolor{blue}{\pi(h)}
            \bigl(f_{ij}(h) + f_{ji}(h) - 2f_i f_j\bigr)
        \]
    \end{block}

    \vspace{0.3cm}
    \textbf{Remarks:}
    \begin{itemize}
        \item Inflation factor \textcolor{blue}{$1/\pi$} at lag 0: fewer effective observations
        \item Serial dependence in $(O_t)$ enters via $\textcolor{blue}{\pi(h)} = E[O_t O_{t+h}]$
        \item Reduces to $\boldsymbol{\Sigma}$ when $\pi = 1$ (no missing data)
    \end{itemize}
\end{frame}

%------------------------------------------------
\begin{frame}{Marginal Statistics under Missingness}
    \begin{block}{Strategy}
        All results from the complete-data case carry over by replacing 
        $\boldsymbol{\Sigma}$ with the missingness-adjusted covariance matrix $\boldsymbol{\Sigma}^*$.
    \end{block}

    \vspace{0.3cm}

    \textbf{IOV:} Same delta-method expansion as before, with $\boldsymbol{\Sigma}$ replaced by $\boldsymbol{\Sigma}^*$.

    \vspace{0.3cm}

    \textbf{Other statistics (e.g. skewness):}
    The same delta-method arguments apply. 
\end{frame}


%------------------------------------------------

\begin{frame}{IOV under Missingness}
    \begin{columns}[T]
        \begin{column}{0.5\textwidth}
            \includegraphics[width=0.9\textwidth]{../Graphs/IOV_Asymp_Presi.png}
        \end{column}
        \begin{column}{0.5\textwidth}
            \vspace{1em}
            \begin{itemize}
                \item Bias decreases linear in n
                \item Variance estimates approach the asymptotic variance as $n$ increases
                \item Asymptotic bias formula captures the finite-sample bias well, even for moderate $n$
            \end{itemize}
        \end{column}
    \end{columns}
\end{frame}
%================================================
\section{Serial Dependence under Missingness}
%================================================

%------------------------------------------------
\begin{frame}{Estimating Bivariate Probabilities}
\textbf{Need to estimate $f_{ij}(h) = P(X_t \leq s_i, X_{t+h} \leq s_j)$}

\vspace{0.3cm}

\textbf{Corrected estimator:}
\[
\hat{f}^*_{ij}(h) =  \frac{\frac{1}{n-h}\sum_{t=h+1}^{n} O_t\, O_{t-h}\, Z_{t,i}\, Z_{t-h,j}}{\frac{1}{n+h} \sum_{t=h+1}^{n} O_t\, O_{t-h}}
\]

$\Rightarrow$ divide by the proportion of \textbf{jointly observed}~($\frac{1}{n+h} \sum_{t=h+1}^{n} O_t\, O_{t-h}$) pairs at lag $h$
\end{frame}

%------------------------------------------------
\begin{frame}{Asymptotic Distribution of $\hat{\kappa}_{\mathrm{ord}}(h)$}

\vspace{0.3cm}
\textbf{CLT + Delta method:}
The corrected estimators of $\boldsymbol{f}$ and $\boldsymbol{f}(h)$ are jointly asymptotically normal, hence (continuous mapping theorem)
\[
\sqrt{n}\,\bigl(\hat{\kappa}_{\mathrm{ord}}(h) - \kappa_{\mathrm{ord}}(h)\bigr)
\;\xrightarrow{d}\;
\mathcal{N}(0, \sigma_\kappa^2)
\]
\vspace{0.3cm}

\textbf{Challenge:}
Higher-order moments of $(X_t)$ are in $\sigma_\kappa^2$.

\vspace{0.3cm}

$\Rightarrow$ We therefore consider the \textbf{null hypothesis of i.i.d. observations}, where $f_{ij}(h) = f_i f_j$, and test on deviation from that.



\vspace{0.3cm}

\end{frame}
%------------------------------------------------
\begin{frame}{Asymptotic Null Distribution of $\hat{\kappa}_{\mathrm{ord}}(h)$}

\vspace{0.3cm}

\begin{block}{Theorem}
Under $H_0$ and the previous assumptions on $(O_t)$ and $X_t$, we can compute $\sigma_\kappa^2$ in closed form:
\[
\sigma^2_\kappa =
\frac{\dfrac{1}{\textcolor{blue}{\pi(h)}}\displaystyle\sum_{i,j=0}^{m-1}
(f_{\min\{i,j\}}-f_i f_j)
\!\left(f_{\min\{i,j\}} + \left(1+\dfrac{2\textcolor{blue}{\pi(h,2h)}}{\textcolor{blue}{\pi(h)}} - \dfrac{4\textcolor{blue}{\pi(h)}}{\textcolor{blue}{\pi}}\right)f_if_j\right)}
{\left(\displaystyle\sum_{k=0}^{m-1}f_k(1-f_k)\right)^2}
\]
with $\textcolor{blue}{\pi(h,2h)} = E[O_t O_{t+h} O_{t-h}]$.
\end{block}
\end{frame}

%------------------------------------------------
\begin{frame}{Bias Correction for $\hat{\kappa}_{\mathrm{ord}}(h)$}

\textbf{Second-order Taylor expansion yields the finite-sample bias under $H_0$:}
\[
\mathrm{Bias}\!\left(n\hat{\kappa}_{\mathrm{ord}}(h)\right) \approx -\frac{1}{\pi}
\]

\vspace{0.4cm}

\textbf{Bias-corrected estimator:}
\[
\hat{\kappa}^{\mathrm{bc}}_{\mathrm{ord}}(h) := \hat{\kappa}_{\mathrm{ord}}(h) + \frac{1}{n\pi}
\]

\vspace{0.4cm}

\textbf{Remarks:}
\begin{itemize}
    \item Bias increases as $\pi \downarrow$ (more missingness)
    \item Under complete data ($\pi = 1$): reduces to the known $-1/n$ bias
\end{itemize}
\end{frame}


%------------------------------------------------
\begin{frame}{Asymptotic Distribution of $\hat{\kappa}_{\mathrm{ord}}(h)$}
\vspace{0.4cm}
\begin{columns}
    \begin{column}{0.5\textwidth}
        \textbf{Under $H_0$}
        \includegraphics[width=0.8\textwidth]{../Graphs/Kappa_H_0.png}
    \end{column}
    \begin{column}{0.5\textwidth}
        \textbf{Under $H_A$}{\tiny(95$\%$ confidence intervals)}
        \includegraphics[width=0.8\textwidth]{../Graphs/kappa_H_A.png}
    \end{column}    
\end{columns}

\end{frame}


%================================================
\section{Simulation Study}
%================================================

%------------------------------------------------
\begin{frame}{Simulation Goals and Setup}
\textbf{Three goals:}
\begin{enumerate}
    \item Assess finite-sample accuracy of asymptotic distributional properties
    \item Examine convergence under serially dependent missingness
    \item Study robustness to violations of the MCAR assumption
\end{enumerate}

\end{frame}
%------------------------------------------------

\begin{frame}{Data Generating Process: BinAR(1)}

\textbf{Goal:} Simulate ordinal time series with controllable dependence.

\vspace{0.3cm}

\textbf{Idea:}
A discrete autoregressive process on $\{0,\ldots,m\}$ with binomial transitions.

\vspace{0.4cm}

\textbf{Definition (intuition):}
\[
X_t = B(X_{t-1}, \alpha) + B(m - X_{t-1}, \beta)
\]

\begin{itemize}
    \item $X_t \in \{0,\ldots,m\}$ (ordinal / categorical structure)
    \item $p$ controls the marginal distribution: $X_t \sim \mathrm{Bin}(m,p)$
    \item $r$ controls serial dependence
    \begin{itemize}
        \item $r=0$: i.i.d.\ case
        \item $r>0$: positive dependence
        \item $r<0$: negative dependence
    \end{itemize}
\end{itemize}

\vspace{0.3cm}

$\Rightarrow$ Flexible benchmark process with closed-form marginals and dependence structure.
\end{frame}
%------------------------------------------------
\begin{frame}{BinAR(1) with varying degree of serial dependence}
    \vspace{-0cm}
    \hspace*{-0.6cm}%
    \includegraphics[width=1.1\textwidth]{../Graphs/kappa_to_h.png}
    \vfill
\end{frame}
%------------------------------------------------

\begin{frame}{Simulation Scenarios}
\textbf{Two scenarios chosen to differ qualitatively:}

\vspace{0.3cm}

\begin{columns}
\begin{column}{0.48\textwidth}
\textbf{Scenario A}\\
$m=3$, $p=0.20$, $r=0.35$
\begin{itemize}
    \item Few categories
    \item Skewed marginal distribution
    \item Moderate serial dependence
\end{itemize}
\end{column}
\begin{column}{0.48\textwidth}
\textbf{Scenario B}\\
$m=10$, $p=0.45$, $r=0.50$
\begin{itemize}
    \item Many categories
    \item Near-symmetric distribution
    \item Strong serial dependence
\end{itemize}
\end{column}
\end{columns}

\vspace{0.4cm}

\textbf{Design:}
\begin{itemize}
    \item Observation rates: $\pi \in \{1, 0.75\}$
    \item Sample sizes: $n \in \{50, 100, 250, 500, 1000\}$
    \item $B = 1{,}000$ Monte Carlo replications
\end{itemize}
\end{frame}

%------------------------------------------------
\begin{frame}{Missingness Mechanisms in the Simulation}
\textbf{Four observation processes $(O_t)$ are studied:}

\begin{enumerate}
    \item $\boldsymbol{O_{t} \perp X_t} \textbf{ and no serial dependence} $: $O_t \overset{\mathrm{i.i.d.}}{\sim} \mathrm{Bernoulli}(\pi)$ \\ {\small Baseline; satisfies all theoretical assumptions.}
    
    \vspace{0.3cm}
    
    \item $\boldsymbol{O_{t} \perp X_t}$ \textbf{ and serial dependence}: $(O_t)$ follows BinAR(1) with $m=1$ \\ {\small Serial dependence in missingness.}
    
    \vspace{0.3cm}
    
    \item \textbf{Dependence between $O_t$ and $X_t$}: Misssingness is state-dependent \\ {\small Higher/ Lower Categories are more likely to be observed.} 

    \vspace{0.3cm}
    
\end{enumerate}
\end{frame}

%------------------------------------------------

\begin{frame}{MNAR vs.\ MCAR: IOV Bias}
    \begin{columns}[T]
        \begin{column}{0.58\textwidth}
            \includegraphics[width=\textwidth, height = 6cm]{../Graphs/MAR_IOV_Presi.png}
        \end{column}
        \begin{column}{0.38\textwidth}
            {\tiny \textcolor{blue!30}{MNAR — increasing}, \textcolor{red!30}{MNAR — decreasing} and MCAR}\\
            \vspace{1em}
            \begin{itemize}
                \item MNAR is biased and not consistent
            \end{itemize}
        \end{column}
    \end{columns}
\end{frame}

%------------------------------------------------

% \begin{frame}{Performance Measures}
% \textbf{For each estimator $\hat{\theta}$, we compute across $B$ replications:}

% \vspace{0.3cm}

% \begin{itemize}
%     \item \textbf{Mean bias:} $\frac{1}{B}\sum_{b=1}^B (\hat{\theta}_b - \theta)$ \\
%     {\small Compared against theoretical $O(n^{-1})$ formula.}
    
%     \vspace{0.3cm}
    
%     \item \textbf{$n$-scaled bias:} $n \cdot \frac{1}{B}\sum_{b=1}^B (\hat{\theta}_b - \theta)$ \\
%     {\small Should converge to the theoretical bias limit as $n \to \infty$.}
    
%     \vspace{0.3cm}
    
%     \item \textbf{Standard deviation:} compared against asymptotic SD $\sqrt{\sigma^2_\theta/n}$
% \end{itemize}

% \vspace{0.4cm}

% \textbf{Theoretical benchmarks:}
% \begin{itemize}
%     \item IOV bias limit: $-\frac{4}{mn}\,\mathrm{tr}(\boldsymbol{\Sigma}^*)$
%     \item $\kappa$ bias limit (under $H_0$): $-\frac{1}{n\pi}$
% \end{itemize}
% \end{frame}

%================================================
\section{Outlook}
%================================================

%------------------------------------------------
\begin{frame}{Outlook}
    \begin{columns}[T]
        \begin{column}{0.48\textwidth}
            \textbf{Remaining work}\\[0.5em]
            \begin{itemize}
                \item Nominal data — transfer of results 
                from ordinal to nominal case
                \item Simulation study — finite-sample 
                validation of asymptotic results of $\kappa(h)$
                \item Analysis of impact of 
                violations of the MCAR assumption
            \end{itemize}
        \end{column}
        \hfill
        \begin{column}{0.48\textwidth}
            \textbf{Open research questions}\\[0.5em]
            \begin{itemize}
                \item Asymptotic theory given dependence 
                of $(O_t)$ on past states of $(X_t)$ (MAR)
                \item Extensions —  
                multivariate categorical processes
                \item Applications — credit ratings, 
                longitudinal survey data
            \end{itemize}
        \end{column}
    \end{columns}
\end{frame}
\end{document}
